/* Dummy version of inttypes.h that should be in the compiler's standard library */
/* Older versions of MSVC don't have inttypes.h, but they do have stdint.h, which is sufficient for use by HDF5 */
#include <stdint.h>